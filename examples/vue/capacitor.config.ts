import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.oidc.vue',
  appName: 'Capacitor OIDC Vue',
  webDir: 'dist',
};

export default config;
