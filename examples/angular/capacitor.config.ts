import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.oidc.angular',
  appName: 'Capacitor OIDC Angular',
  webDir: 'dist/browser',
};

export default config;
